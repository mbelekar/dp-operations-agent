#!/usr/bin/env bash
# Creates a virtualenv (if missing) and installs the project with dev dependencies.
set -euo pipefail
cd "$(dirname "$0")/.."

if [ ! -d ".venv" ]; then
  python3 -m venv .venv
fi

source .venv/bin/activate
pip install -q --upgrade pip
pip install -e ".[dev]"
