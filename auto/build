#!/usr/bin/env bash
# Creates .venv and installs the project with dev dependencies, exactly as
# pinned in uv.lock. Requires uv (https://docs.astral.sh/uv/).
set -euo pipefail
cd "$(dirname "$0")/.."

uv sync --frozen
